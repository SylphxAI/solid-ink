# Configure export conditions priority
# Use browser condition only (not development) to avoid server.js
conditions = ["browser", "import", "module", "default"]

# Preload SolidJS plugin for JSX transformation
preload = ["./preload-solid.ts"]
